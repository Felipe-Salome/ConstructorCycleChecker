plugins {
    id 'java'
    id 'application'
}

group = 'constructorcyclechecker'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
    testImplementation platform('org.junit:junit-bom:5.10.0')
    testImplementation 'org.junit.jupiter:junit-jupiter'
}

application {
    mainClass = 'constructorcyclechecker.Main'
}

test {
    useJUnitPlatform()
}